---
import SlideUp from '@/assets/symbols/SlideUp.astro'
import Title from '@/components/Title.astro'
import WorkItem from '@/components/WorkItem.astro'
import { homePageWorksTranslations } from '@/i18n/translations'

const { title, works } = homePageWorksTranslations(Astro.url)
---

<section
  id='work'
  class={`
  relative bg-section-bg-light dark:bg-section-bg-dark
    border-2 border-section-primary-border-light dark:border-section-primary-border-dark
    flex flex-col
  `}
  transition:name='work'
>
  <SlideUp
    class='absolute size-6 top-0 right-0 m-4 z-10 text-black/40 dark:text-white/20'
  />
  <Title title={title} />
  <ol
    class='border-s-2 border-neutral-400 dark:border-neutral-800 overflow-auto ml-2'
  >
    {works.map((work: object[]) => <WorkItem {...work} />)}
  </ol>
</section>

<style>
  ol {
    scrollbar-width: none;
  }
</style>
