---
import ArrowUp from '@/assets/symbols/ArrowUp.astro'

const { id, title, date, tag, author } = Astro.props

const titleCleaned = title
  .normalize('NFD')
  .replace(/[\u0300-\u036f]/g, '')
  .replace(/[^\w\s-]/g, '')
  .replaceAll(' ', '-')
  .toLowerCase()

const postUrl = id + '-' + titleCleaned
---

<div
  id=`post-${id}`
  data-tag=`post-${tag}`
  class={`
    posts
    hover:cursor-pointer
    card max-h-auto relative overflow-hidden w-full h-full rounded-md m-2 group
  `}
>
  <h2
    class='text-lg px-6 pt-6 pb-3 md:px-3 md:pt-3 md:pb-1.5 text-primary-light group-hover:text-secondary-ligh dark:text-primary-dark group-hover:dark:text-white'
  >
    {title}
  </h2>

  <div class='flex items-end z-0 pl-8 md:pl-4'>
    <p
      transition:name={postUrl}
      class='cust-font font-bold text-6xl text-secondary-light dark:text-secondary-dark'
    >
      {id}
    </p>
    <p
      class='mb-1 ml-2 opacity-50 group-hover:opacity-80 text-secondary-light dark:text-secondary-dark'
    >
      {tag}
    </p>
  </div>

  <p
    class='cust-font absolute top-0 right-0 m-2 opacity-50 group-hover:opacity-80 text-primary-light group-hover:text-secondary-light dark:text-secondary-dark group-hover:dark:text-white'
  >
    {date}
  </p>

  <p
    class='absolute bottom-0 font-light text-xs right-0 m-2 opacity-50 group-hover:opacity-90 text-white dark:text-tertiary-dark'
  >
    @{author}
  </p>

  <a href={`posts/${postUrl}`} class='absolute top-0 h-full w-full'></a>

  <div
    class='absolute bottom-[-100%] w-full flex items-center justify-center pointer-events-none -z-10 group group-hover:bottom-0 group-hover:z-30 transition-all duration-500 ease-in-out'
  >
    <button class='flex flex-col items-center pb-4 justify-end w-full h-full'>
      <ArrowUp class='size-4' />
    </button>
  </div>
</div>

<style>
  /* TO-DO: IMPORT FONT */
  @import url('https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&display=swap');
  .cust-font {
    font-family: 'Lora', arial;
  }
</style>

<script>
  import { blogCardAssignColor } from '@/data/blogCardAssignColor'
  import { toggleBkgGradient } from '@/data/toggleBkgGradient'

  toggleBkgGradient('class', 'posts', blogCardAssignColor, {
    dark: 'bg-gradient-to-b from-[#171717] via-30% via-[#171717] to-100% border',
    light:
      'bg-gradient-to-b from-[#E2E8F0] via-10% via-[#E2E8F0] to-100% border',
  })
</script>
